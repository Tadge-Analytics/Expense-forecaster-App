library(tidyverse)
library(lubridate)
library(readxl)
library(plotly)
library(DT)
library(openxlsx)
library(shinyWidgets)

# a little change

# Run
# 
#   git config --global user.email "you@example.com"
#   git config --global user.name "Your Name"


# usethis::use_git_config(user.name = "Julian Tagell", user.email = "julian@tadge-analytics.com.au")
